let a = 1;

a = a + 1;  // binário | infix = um operador no meio de dois operandos (entre)
a += 1; 
a++; // unário | postfix = operador depois do operando
++a; // outra possibilidade | prefix = operador antes do operando

a--; // postfix
--a; // prefix

console.log(a);