let quantidade = 0;

// indeterminada de repetições
while (Math.random() < 0.97){
    // console.log('< 0.9');
    quantidade++;
}

// console.log(Math.random()); // gera número aleatório entre 0 e 1 (o 1 não é sorteado)

console.log('Qtde: ' + quantidade);