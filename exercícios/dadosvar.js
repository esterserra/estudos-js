// anatomia - var ou let + identificador da variável + valor + ;

var nome = "Caneta";
var quantidade = 10; // armazenar dentro da variável o número 10. o sinal de igual significa atribuição
var preco = 6.4;
var imposto = 1.5;
var precoFinal = preco + imposto; // em caso de duas palavras, a primeira letra da segunda palavra fica em maiúsculo

console.log(nome);
console.log(quantidade); // valor direto = valor literal
console.log(preco); 
console.log(imposto);
console.log(preco + imposto); // ou console.log(precoFinal);

nome = "Caneta BIC"
console.log(nome);

// em js o número quebrado é separado por ponto
// a forma mais atualizada de usar uma variável em js é substituindo o var por let